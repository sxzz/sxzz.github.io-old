<?xml version="1.0" encoding="utf-8"?>
<feed xmlns="http://www.w3.org/2005/Atom">
  <title>三咲智子</title>
  <icon>https://blog.sxzz.moe/avatar.png</icon>
  
  <link href="https://blog.sxzz.moe/atom.xml" rel="self"/>
  
  <link href="https://blog.sxzz.moe/"/>
  <updated>2022-03-30T04:25:04.116Z</updated>
  <id>https://blog.sxzz.moe/</id>
  
  <author>
    <name>三咲智子</name>
    
  </author>
  
  <generator uri="https://hexo.io/">Hexo</generator>
  
  <entry>
    <title>Element Plus 源码分析——构建与代码风格</title>
    <link href="https://blog.sxzz.moe/element-plus/"/>
    <id>https://blog.sxzz.moe/element-plus/</id>
    <published>2022-03-30T04:20:57.286Z</published>
    <updated>2022-03-30T04:25:04.116Z</updated>
    
    <content type="html"><![CDATA[<h2 id="项目概述"><a href="#项目概述" class="headerlink" title="项目概述"></a>项目概述</h2><ul><li>官网：<a href="https://element-plus.org/">https://element-plus.org/</a></li><li>GitHub 仓库：<a href="https://github.com/element-plus/element-plus">https://github.com/element-plus/element-plus</a></li><li>包管理：<a href="https://pnpm.io/workspaces">pnpm workspace</a></li><li>组件代码：<a href="https://www.typescriptlang.org/">TypeScript</a>、<a href="https://vuejs.org/api/sfc-spec.html">Vue SFC</a>（Vue 单文件组件）</li><li>样式：<a href="https://sass-lang.com/">Scss</a>、<a href="https://developer.mozilla.org/zh-CN/docs/Web/CSS/var">CSS var</a></li><li>单元测试：<a href="https://jestjs.io/">Jest</a>、<a href="https://vitest.dev/">Vitest</a></li><li>构建：<a href="https://rollupjs.org/">Rollup</a>、<a href="https://esbuild.github.io/">esbuild</a>、<a href="https://www.typescriptlang.org/">TypeScript</a>、<a href="https://gulpjs.com/">Gulp</a></li><li>代码风格：<a href="https://eslint.org/">ESLint</a>、<a href="https://prettier.io/">Prettier</a></li></ul><p>本文章基于 <a href="https://github.com/element-plus/element-plus/tree/2.1.4">Element Plus v2.1.4</a>。如有错误之处，欢迎指正。</p><h2 id="构建"><a href="#构建" class="headerlink" title="构建"></a>构建</h2><ul><li>Keywords: <code>Vue SFC</code>, <code>Rollup</code>, <code>Bundleless</code>, <code>TypeScript</code>, <code>ESM</code>, <code>CommonJS</code>/<code>CJS</code>, <code>UMD</code></li></ul><p>这部分在一个单独的包 <code>@element-plus/build</code>，代码位于 <a href="https://github.com/element-plus/element-plus/tree/2.1.4/internal/build"><code>/internal/build</code></a>。</p><p>使用到的工具有：<code>rollup</code>、<code>unbuild</code>、<code>esbuild</code>、<code>gulp</code>、<code>ts-morph</code>、<code>fast-glob</code> 等。如需深度理解，建议先自行了解这些包后阅读本章节。</p><h3 id="目录结构"><a href="#目录结构" class="headerlink" title="目录结构"></a>目录结构</h3><h4 id="源码"><a href="#源码" class="headerlink" title="源码"></a>源码</h4><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br></pre></td><td class="code"><pre><span class="line">internal&#x2F;build</span><br><span class="line">├── build.config.ts        # unbuild 配置文件</span><br><span class="line">├── dist                    # 构建产物</span><br><span class="line">├── gulpfile.ts             # 构建脚本</span><br><span class="line">├── package.json</span><br><span class="line">├── src</span><br><span class="line">│   ├── build-info.ts       # 构建信息</span><br><span class="line">│   ├── constants.ts        # 一些常量</span><br><span class="line">│   ├── index.ts            # 入口文件</span><br><span class="line">│   ├── plugins             # 插件</span><br><span class="line">│   │   └── element-plus-alias.ts  # 导入别名</span><br><span class="line">│   ├── tasks</span><br><span class="line">│   │   ├── full-bundle.ts  # 构建完整产物</span><br><span class="line">│   │   ├── helper.ts       # 生成 WebStorm 提示文件</span><br><span class="line">│   │   ├── index.ts</span><br><span class="line">│   │   ├── modules.ts      # 构建 bundleless 产物</span><br><span class="line">│   │   └── types-definitions.ts # 生成 d.ts 文件</span><br><span class="line">│   ├── type-safe.json      # 「类型安全」列表</span><br><span class="line">│   └── utils               # 工具函数</span><br><span class="line">│       ├── gulp.ts</span><br><span class="line">│       ├── index.ts</span><br><span class="line">│       ├── log.ts</span><br><span class="line">│       ├── paths.ts</span><br><span class="line">│       ├── pkg.ts</span><br><span class="line">│       ├── process.ts</span><br><span class="line">│       └── rollup.ts</span><br><span class="line">├── tsconfig.json</span><br><span class="line">└── vue-jest-transformer.js</span><br></pre></td></tr></table></figure><h4 id="构建产物"><a href="#构建产物" class="headerlink" title="构建产物"></a>构建产物</h4><p><a href="https://cdn.jsdelivr.net/npm/element-plus@latest/">在线预览</a></p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br></pre></td><td class="code"><pre><span class="line">dist&#x2F;</span><br><span class="line">├── element-plus         # 最终构建产物</span><br><span class="line">│   ├── README.md</span><br><span class="line">│   ├── attributes.json</span><br><span class="line">│   ├── dist             # 完整构建产物</span><br><span class="line">│   ├── es               # bundleless 构建产物，ESM 格式</span><br><span class="line">│   ├── global.d.ts      # 供 Volar 使用的全局组件类型</span><br><span class="line">│   ├── lib              # bundleless 构建产物，CJS 格式</span><br><span class="line">│   ├── package.json</span><br><span class="line">│   ├── tags.json</span><br><span class="line">│   ├── theme-chalk      # 样式产物</span><br><span class="line">│   └── web-types.json</span><br><span class="line">└── types                # 类型声明产物</span><br></pre></td></tr></table></figure><h3 id="构建流程"><a href="#构建流程" class="headerlink" title="构建流程"></a>构建流程</h3><p>首先可以看到在 <a href="https://github.com/element-plus/element-plus/blob/2.1.4/internal/build/package.json"><code>package.json</code></a> 文件中的 <a href="https://github.com/element-plus/element-plus/blob/2.1.4/internal/build/package.json#L10"><code>start</code></a> 脚本，启动 Gulp 运行 <a href="https://github.com/element-plus/element-plus/blob/2.1.4/internal/build/gulpfile.ts"><code>gulpfile.ts</code></a> 文件。这将运行 <code>gulpfile</code> 的默认导出。在 <code>gulpfile.ts</code> 文件中找到 <code>export default</code> <a href="https://github.com/element-plus/element-plus/blob/2.1.4/internal/build/gulpfile.ts#L49-L67">相关代码</a>就是构建流程了。</p><h4 id="0-clean-清理产物"><a href="#0-clean-清理产物" class="headerlink" title="0. clean 清理产物"></a>0. <code>clean</code> 清理产物</h4><p>调用一个 <code>run</code> 函数，将会在项目根目录运行 <code>pnpm run clean</code>，其作用为删除根目录下的 <code>dist</code> 文件夹，并且运行 <code>/packages</code> 目录下每个包的 <code>clean</code> 脚本。</p><h4 id="1-createOutput-创建构建产物的目录"><a href="#1-createOutput-创建构建产物的目录" class="headerlink" title="1. createOutput 创建构建产物的目录"></a>1. <code>createOutput</code> 创建构建产物的目录</h4><p>创建 <code>/dist/element-plus</code> 文件夹。在上一步中，我们把 <code>dist</code> 目录整个删掉了，为了保证目录存在，需要把之前的 <code>dist</code> 目录创建回来。</p><h4 id="2-parallel"><a href="#2-parallel" class="headerlink" title="2. parallel"></a>2. <a href="https://github.com/element-plus/element-plus/blob/2.1.4/internal/build/gulpfile.ts#L53">parallel</a></h4><p>这是多个并行的任务，因为构建每种不同类型的产物是可以同时进行的。</p><h4 id="2-1-buildModules-构建-Bundless-产物"><a href="#2-1-buildModules-构建-Bundless-产物" class="headerlink" title="2.1 buildModules 构建 Bundless 产物"></a>2.1 <code>buildModules</code> 构建 Bundless 产物</h4><p>将开启一个新进程开始执行 <a href="https://github.com/element-plus/element-plus/blob/2.1.4/internal/build/src/tasks/modules.ts"><code>./src/tasks/modules.ts</code></a> 中的 <a href="https://github.com/element-plus/element-plus/blob/2.1.4/internal/build/src/tasks/modules.ts#L22"><code>buildModules</code></a> 函数。最后将在 <code>/dist/element-plus</code> 下生成 <code>es</code> 与 <code>lib</code> 两个文件夹，分别为 ESM 与 CJS 两种格式。</p><h4 id="2-2-buildFullBundle-构建完整产物"><a href="#2-2-buildFullBundle-构建完整产物" class="headerlink" title="2.2 buildFullBundle 构建完整产物"></a>2.2 <code>buildFullBundle</code> 构建完整产物</h4><p>与上一步类似，将执行 <a href="https://github.com/element-plus/element-plus/blob/2.1.4/internal/build/src/tasks/full-bundle.ts"><code>./src/tasks/full-bundle.ts</code></a> 中的 <a href="https://github.com/element-plus/element-plus/blob/2.1.4/internal/build/src/tasks/full-bundle.ts#L138"><code>buildFullBundle</code></a> 函数。但这步比上一步多一个<a href="https://github.com/element-plus/element-plus/blob/2.1.4/internal/build/src/tasks/full-bundle.ts#L88">构建 <code>locale</code></a>，因为需要考虑在 CDN 场景下导入不同语言。另外，用户导入完整包可以不需要使用构建工具 （Vite、Webpack 等），需要我们需要还需要提供压缩版本，以便能够节省加载时间。</p><p><a href="https://github.com/element-plus/element-plus/blob/2.1.4/internal/build/src/tasks/full-bundle.ts#L135"><code>buildFull</code></a> 函数就提供了一个参数 <code>minify</code>，以便可以并行执行两个任务——一个压缩版本和一个不压缩的版本。</p><p><a href="https://github.com/element-plus/element-plus/blob/2.1.4/internal/build/src/tasks/full-bundle.ts#L30"><code>buildFullEntry</code></a> 函数就是构建完整产物的函数。除了没有使用 <code>preserveModules</code> 选项，与 <code>buildModules</code> 基本一致。提供了 UMD 与 ESM 两种格式。</p><p><a href="https://github.com/element-plus/element-plus/blob/2.1.4/internal/build/src/tasks/full-bundle.ts#L88"><code>buildFullLocale</code></a> 函数用来构建语言包。为 <code>packages/locale/**/*.ts</code> 下的每个文件同时构建 UMD 与 ESM 两种格式的产物。</p><p>同时，开启 <code>minify</code> 参数时会开启 <code>sourceMap</code> 选项，为开发时提供原始源码用来调试。</p><h4 id="2-3-generateTypesDefinitions-生成-d-ts-文件"><a href="#2-3-generateTypesDefinitions-生成-d-ts-文件" class="headerlink" title="2.3 generateTypesDefinitions 生成 .d.ts 文件"></a>2.3 <code>generateTypesDefinitions</code> 生成 <code>.d.ts</code> 文件</h4><p>为了用户开发时提供类型支持，还需要同时生成 <code>.d.ts</code>。将执行 <a href="https://github.com/element-plus/element-plus/blob/2.1.4/internal/build/src/tasks/types-definitions.ts"><code>./src/tasks/types-definitions.ts</code></a> 中的 <a href="https://github.com/element-plus/element-plus/blob/2.1.4/internal/build/src/tasks/types-definitions.ts#L34"><code>generateTypesDefinitions</code></a> 函数。最后将 <code>.d.ts</code> 写入到 <code>/dist/types</code> 目录中。</p><h4 id="2-4-buildHelper-生成-IDE-支持"><a href="#2-4-buildHelper-生成-IDE-支持" class="headerlink" title="2.4 buildHelper 生成 IDE 支持"></a>2.4 <code>buildHelper</code> 生成 IDE 支持</h4><p>对于 WebStorm 与 Vetur，需要生成 <code>web-types.json</code> 与 <code>tags.json</code>、<code>attributes.json</code> 文件供插件分析，最终提供代码提示。使用了 <a href="https://github.com/tolking/components-helper">components-helper</a> 分析文档 Markdown 生成。</p><p>开发 Vue 3 应用还是推荐使用 VSCode + Volar，这部分可能以后会被移除。因为用文档生成上述文件并不可靠。</p><h4 id="2-5-buildThemeChalk-copyFullStyle-构建并复制样式"><a href="#2-5-buildThemeChalk-copyFullStyle-构建并复制样式" class="headerlink" title="2.5 buildThemeChalk copyFullStyle 构建并复制样式"></a>2.5 <code>buildThemeChalk</code> <code>copyFullStyle</code> 构建并复制样式</h4><p><code>buildThemeChalk</code>：这步将先执行 <code>buildThemeChalk</code>，从 Sass 源代码构建为 CSS 产物。相关代码位于 <a href="https://github.com/element-plus/element-plus/blob/2.1.4/packages/theme-chalk/gulpfile.ts"><code>packages/theme-chalk/gulpfile.ts</code></a>。最终把产物从 <code>packages/theme-chalk/dist</code> 复制到 <code>dist/element-plus/theme-chalk</code>。</p><p><code>copyFullStyle</code>：将复制 <code>dist/element-plus/theme-chalk/index.css</code> 到 <code>dist/element-plus/dist/index.css</code>。把完整的 CSS 样式复制到「完整构建产物」的目录中，方便 CDN 引入。</p><h4 id="3-copyTypesDefinitions-复制-d-ts-文件"><a href="#3-copyTypesDefinitions-复制-d-ts-文件" class="headerlink" title="3. copyTypesDefinitions 复制 .d.ts 文件"></a>3. <code>copyTypesDefinitions</code> 复制 <code>.d.ts</code> 文件</h4><p>因为有 ESM 与 CJS 两种格式都需要类型声明文件，所以没有一开始就生成到「最终构建产物」目录。最后将「类型声明产物」的所有文件复制到 <code>dist/element-plus/es</code> 与 <code>dist/element-plus/lib</code> 中即可。</p><p>以上就是所有流程。</p><h3 id="Rollup-插件"><a href="#Rollup-插件" class="headerlink" title="Rollup 插件"></a>Rollup 插件</h3><p>「完整构建产物」与「bundleless 构建产物」都使用到了 Rollup。</p><p>Rollup 是不能解析像 TypeScript、Vue SFC 这些非 JavaScript 的代码，所以需要借助各种插件，来帮助 Rollup 将其转换为 JavaScript 代码。Rollup 也不能解析 CommonJS 格式的代码，还需要插件来帮助 Rollup 解决这些问题。</p><h4 id="unplugin-vue-define-options"><a href="#unplugin-vue-define-options" class="headerlink" title="unplugin-vue-define-options"></a><a href="https://github.com/sxzz/unplugin-vue-define-options"><code>unplugin-vue-define-options</code></a></h4><p>这是我个人写的一个插件。通过这个插件就可以在 Vue 的 <code>&lt;script setup&gt;</code> 中使用 <code>Options API</code> 。Element Plus 需要定义组件 <code>name</code> 属性，这个就可以避免两个 <code>script</code> 标签了。</p><h4 id="vitejs-plugin-vue"><a href="#vitejs-plugin-vue" class="headerlink" title="@vitejs/plugin-vue"></a><a href="https://github.com/vitejs/vite/tree/main/packages/plugin-vue"><code>@vitejs/plugin-vue</code></a></h4><p>Vite 官方插件，把 Vue SFC 编译为 JavaScript 代码。</p><h4 id="vitejs-plugin-vue-jsx"><a href="#vitejs-plugin-vue-jsx" class="headerlink" title="@vitejs/plugin-vue-jsx"></a><a href="https://github.com/vitejs/vite/tree/main/packages/plugin-vue-jsx"><code>@vitejs/plugin-vue-jsx</code></a></h4><p>Vite 官方插件，支持 Vue JSX 语法。</p><h4 id="rollup-plugin-node-resolve"><a href="#rollup-plugin-node-resolve" class="headerlink" title="@rollup/plugin-node-resolve"></a><a href="https://github.com/rollup/plugins/tree/master/packages/node-resolve"><code>@rollup/plugin-node-resolve</code></a></h4><p>Rollup 官方插件，让 Rollup 支持 Node.js 的<a href="https://nodejs.org/api/modules.html#modules_all_together">解析算法</a>，用于解析 <code>node_modules</code>。</p><p><code>extensions</code> 选项：默认是不包含 <code>.ts</code> 后缀名的，所以需要手动加上。</p><h4 id="rollup-plugin-commonjs"><a href="#rollup-plugin-commonjs" class="headerlink" title="@rollup/plugin-commonjs"></a><a href="https://github.com/rollup/plugins/tree/master/packages/commonjs"><code>@rollup/plugin-commonjs</code></a></h4><p>Rollup 官方插件，用于将 CommonJS 模块转换为 ES6，这样就可以被 Rollup 解析。</p><h4 id="rollup-plugin-esbuild"><a href="#rollup-plugin-esbuild" class="headerlink" title="rollup-plugin-esbuild"></a><a href="https://github.com/egoist/rollup-plugin-esbuild"><code>rollup-plugin-esbuild</code></a></h4><p>一个强大、高效、好用的插件！用于把 TypeScript 转换为 JavaScript，并可以用于压缩代码、转换语法。基于 <a href="https://esbuild.github.io/">ESBuild</a>，速度极快。</p><p><code>minify</code> 选项：是否压缩代码。</p><p><code>target</code> 选项：向下兼容低版本浏览器。Element Plus 最低兼容 <code>ES2018</code>，但在开发中会使用到一些新语法（比如 <a href="https://developer.mozilla.org/zh-CN/docs/Web/JavaScript/Reference/Operators/Optional_chaining">Optional chaining</a> 等），所以需要借助 ESBuild 的能力，把语法糖转换为能兼容 ES2018 的代码。</p><p><code>loaders</code> 选项：通过 <code>@vitejs/plugin-vue</code> 插件编译后，Vue SFC 就是普通的 JavaScript 代码了，但是其后缀还是 <code>.vue</code> 。所以需要添加 <code>&#39;.vue&#39;: &#39;ts&#39;,</code>，把 <code>.vue</code> 文件看作普通的 <code>.js</code> 文件。</p><h3 id="Rollup-配置"><a href="#Rollup-配置" class="headerlink" title="Rollup 配置"></a>Rollup 配置</h3><h4 id="external"><a href="#external" class="headerlink" title="external"></a><a href="https://esbuild.github.io/api/#external"><code>external</code></a></h4><p>当构建 bundless 时，将 <code>dependencies</code> 和 <code>peerDependencies</code> 排除在构建产物之外。</p><p>当构建完整产物时，将 <code>peerDependencies</code> （也就是 <code>vue</code>）排除在构建产物之外。</p><h4 id="preserveModules"><a href="#preserveModules" class="headerlink" title="preserveModules"></a><a href="https://rollupjs.org/guide/en/#outputpreservemodules"><code>preserveModules</code></a></h4><p>开启后，构建产物将保持与源码一样的文件结构。</p><p>可以理解为仅把 Vue SFC、TypeScript 等转换成了 JavaScript 代码，其他不变。</p><h3 id="其他代码"><a href="#其他代码" class="headerlink" title="其他代码"></a>其他代码</h3><h4 id="build-config-ts"><a href="#build-config-ts" class="headerlink" title="build.config.ts"></a><a href="https://github.com/element-plus/element-plus/blob/2.1.4/internal/build/build.config.ts"><code>build.config.ts</code></a></h4><p>使用 <a href="https://github.com/unjs/unbuild">unbuild</a> 生成开发时 stub，开发调试用。这样就不用 watch 一直监听文件构建。</p><h4 id="src-type-safe-json"><a href="#src-type-safe-json" class="headerlink" title="src/type-safe.json"></a><a href="https://github.com/element-plus/element-plus/blob/2.1.4/internal/build/src/type-safe.json"><code>src/type-safe.json</code></a></h4><p>因为 Element Plus 的代码目前还有 TypeScript 类型产物，但已经重构完成了部分文件。这个文件就是记录已重构完成的列表。为了在生成 <code>.d.ts</code> 是针对这部分文件做异常处理。</p><h4 id="src-build-info-ts"><a href="#src-build-info-ts" class="headerlink" title="src/build-info.ts"></a><a href="https://github.com/element-plus/element-plus/blob/2.1.4/internal/build/src/build-info.ts"><code>src/build-info.ts</code></a></h4><p>将构建的配置集中写在这个文件中，包括路径、构建格式、后缀名等。</p><h4 id="src-constants-ts"><a href="#src-constants-ts" class="headerlink" title="src/constants.ts"></a><a href="https://github.com/element-plus/element-plus/blob/2.1.4/internal/build/src/constants.ts"><code>src/constants.ts</code></a></h4><p>一些常量。如果想自行构建并修改一些参数，可以修改此文件。</p><h4 id="src-plugins-element-plus-alias-ts"><a href="#src-plugins-element-plus-alias-ts" class="headerlink" title="src/plugins/element-plus-alias.ts"></a><code>src/plugins/element-plus-alias.ts</code></h4><p>解析样式导入的路径。把 <code>&quot;@element-plus/theme-chalk&quot;</code> 替换为 <code>&quot;element-plus/theme-chalk&quot;</code>。</p><h3 id="生成-d-ts-类型定义"><a href="#生成-d-ts-类型定义" class="headerlink" title="生成 .d.ts 类型定义"></a>生成 <code>.d.ts</code> 类型定义</h3><p>代码位于在 <code>generateTypesDefinitions</code> 步骤中。这里使用了 <code>ts-morph</code> 简化 TypeScript 编译调用。首先<a href="https://github.com/element-plus/element-plus/blob/2.1.4/internal/build/src/tasks/types-definitions.ts#L35-L51">创建一个 <code>Project</code></a> 并指定配置，用于覆盖 <code>tsconfig.json</code> 的配置。<code>skipAddingFilesFromTsConfig</code> 设置为 <code>false</code> 的目的是，我们需要手动选择文件到编译器中。<a href="https://github.com/element-plus/element-plus/blob/2.1.4/internal/build/src/tasks/types-definitions.ts#L53-L60">通过 <code>glob</code> 匹配所有源码文件</a>，并<a href="https://github.com/element-plus/element-plus/blob/2.1.4/internal/build/src/tasks/types-definitions.ts#L61-L66">过滤掉仅用于测试与开发的文件</a>。获取到文件列表后，需要把文件添加到 TypeScript 编译器中。</p><p>值得注意的是 Vue SFC 不可以直接添加到编译器中，因为 TypeScript 无法解析它。所以需要使用 <code>vue/compiler-sfc</code> 中的 <code>parse</code> 和 <code>compileScript</code> 把 <code>script</code> 块中的代码提取出来，并编译 <code>&lt;script setup&gt;</code> 的代码。之后再添加到编译器中。</p><p>另外，由于构建产物的目录结构与我们实际的源码目录结构不一样。所以这里要特殊处理下——<a href="https://github.com/element-plus/element-plus/blob/2.1.4/internal/build/src/tasks/types-definitions.ts#L97-L102">手动读取文件内容并添加到编译器中</a>。</p><p>还需要工具「类型安全」列表中的文件进行匹配，如果列表中的文件有类型错误，则直接报错。可以防止意外造成的 TypeScript 错误。后续将陆续完成代码重构，保证 0 TypeScript 错误。</p><p>最后就是把编译好的 <code>.d.ts</code> 文件写入到构建目录即可。</p><h3 id="未来"><a href="#未来" class="headerlink" title="未来"></a>未来</h3><p>我写了一个<a href="https://github.com/sxzz/element-plus-next/tree/main/packages/build-cli">新的构建流程</a>，将来可能在 Element Plus 3 中使用到。全程使用 ESBuild 构建，速度应该会快很多。另外，未来可能将只构建 ESM 和 IIFE 格式的版本。因此，Bundleless 构建产物也可以去掉。</p><h2 id="代码风格"><a href="#代码风格" class="headerlink" title="代码风格"></a>代码风格</h2><p>这部分在一个单独的包 <code>@element-plus/eslint-config</code>，代码位于 <a href="https://github.com/element-plus/element-plus/tree/2.1.4/internal/eslint-config"><code>/internal/eslint-config</code></a>。</p><p>如果想给 Element Plus 贡献代码，请务必确保代码风格没有问题。可以运行 <code>pnpm run lint</code> 来检查是否存在问题，并使用 <code>pnpm run lint:fix</code> 自动修复问题。</p><h3 id="ESLint"><a href="#ESLint" class="headerlink" title="ESLint"></a>ESLint</h3><p>Element Plus 使用 ESLint 来管理代码风格，同时使用了多个插件来支持不同的文件类型。支持的文件类型有：JavaScript、TypeScript、JSX/TSX、Vue SFC、Markdown、JSON、JSON5，详见 <a href="https://github.com/element-plus/element-plus/blob/2.1.4/.vscode/settings.json#L17-L27"><code>.vscode/settings.json</code></a> 的配置项 <code>eslint.validate</code>。</p><h3 id="Prettier"><a href="#Prettier" class="headerlink" title="Prettier"></a>Prettier</h3><p>总体上使用 Prettier 来约束代码风格，详见 <a href="https://github.com/element-plus/element-plus/blob/2.1.4/.prettierrc">Prettier 配置文件</a>。并使用 Prettier 插件和预设规则与 ESLint 集成。</p><h2 id="其他"><a href="#其他" class="headerlink" title="其他"></a>其他</h2><p>其他代码部分将在后续更新。</p>]]></content>
    
    
      
      
        
        
    <summary type="html">&lt;h2 id=&quot;项目概述&quot;&gt;&lt;a href=&quot;#项目概述&quot; class=&quot;headerlink&quot; title=&quot;项目概述&quot;&gt;&lt;/a&gt;项目概述&lt;/h2&gt;&lt;ul&gt;
&lt;li&gt;官网：&lt;a</summary>
        
      
    
    
    
    
    <category term="web" scheme="https://blog.sxzz.moe/tags/web/"/>
    
    <category term="element-plus" scheme="https://blog.sxzz.moe/tags/element-plus/"/>
    
  </entry>
  
  <entry>
    <title>acme.sh 免费申请 HTTPS 证书</title>
    <link href="https://blog.sxzz.moe/acme-sh/"/>
    <id>https://blog.sxzz.moe/acme-sh/</id>
    <published>2018-07-05T22:00:31.000Z</published>
    <updated>2022-03-30T04:20:50.906Z</updated>
    
    <content type="html"><![CDATA[<p>服务器环境：Ubuntu 16.04 + Nginx</p><a id="more"></a><h2 id="安装-acme-sh"><a href="#安装-acme-sh" class="headerlink" title="安装 acme.sh"></a>安装 acme.sh</h2><p>因为自动需要续期需要重启 nginx，建议使用 root 用户安装</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">curl https://get.acme.sh | sh</span><br></pre></td></tr></table></figure><p>然后再重载一下 <code>.bashrc</code></p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="built_in">source</span> ~/.bashrc</span><br></pre></td></tr></table></figure><h2 id="申请证书"><a href="#申请证书" class="headerlink" title="申请证书"></a>申请证书</h2><p>可以使用两种方式验证：HTTP 和 DNS 验证，我使用的是后者。下面主要介绍 DNS 验证。</p><blockquote><p><strong>acme.sh</strong> 目前支持 cloudflare、dnspod、cloudxns、godaddy 以及 ovh 等数十种解析商的自动集成。</p></blockquote><p>这里我使用的是 DNSPod，登录 DNSPod，在 <a href="https://www.dnspod.cn/console/user/security">安全设置</a> 里面新建一个 API TOKEN。</p><p>创建好了然后：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line"><span class="built_in">export</span> DP_Id=<span class="string">&quot;ID&quot;</span></span><br><span class="line"><span class="built_in">export</span> DP_Key=<span class="string">&quot;Token&quot;</span></span><br><span class="line"></span><br><span class="line">acme.sh --issue --dns dns_dp -d &lt;domain&gt; -d *.&lt;domain&gt; <span class="comment"># 有多个子域名可以写多个-d &lt;domain&gt;.com</span></span><br></pre></td></tr></table></figure><p>为了等待 TXT 记录生效，可能要等待 120 秒左右。</p><p>生成好了之后，ID 和 Token 会自动保存，下次再使用 DNSPod 的时候就不用指定，直接生成就好了。</p><h2 id="安装证书"><a href="#安装证书" class="headerlink" title="安装证书"></a>安装证书</h2><p>我打算把证书保存在 <code>/etc/nginx/ssl/&lt;domain&gt;</code>目录下，所以先创建该目录。</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line">mkdir -p /etc/nginx/ssl/sxzz.moe/ <span class="comment">#直接创建多级目录</span></span><br><span class="line">acme.sh --installcert -d sxzz.moe \</span><br><span class="line">        --key-file /etc/nginx/ssl/sxzz.moe/sxzz.moe.key \</span><br><span class="line">        --fullchain-file /etc/nginx/ssl/sxzz.moe/sxzz.moe.pem \</span><br><span class="line">        --reloadcmd  <span class="string">&quot;service nginx force-reload&quot;</span> <span class="comment"># 证书更新后会自动重载服务器</span></span><br></pre></td></tr></table></figure><p>执行完这些命令后，证书会自动拷贝到指定的位置，并且以后自动更新证书（60 天）就会自动按照这些参数运行。</p><blockquote><p>官方提示：这里用的是 <code>service nginx force-reload</code>，不是 <code>service nginx reload</code>，据测试, <code>reload</code> 并不会重新加载证书，所以用的 <code>force-reload</code>。</p></blockquote><h2 id="配置-Nginx"><a href="#配置-Nginx" class="headerlink" title="配置 Nginx"></a>配置 Nginx</h2><p>这里我使用的是 Nginx 作为 Web 服务器，下面是我的配置，仅供参考。</p><figure class="highlight nginx"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br></pre></td><td class="code"><pre><span class="line"><span class="section">server</span> &#123;</span><br><span class="line">    <span class="attribute">listen</span> <span class="number">443</span> ssl http2;</span><br><span class="line"></span><br><span class="line">    <span class="attribute">ssl_certificate</span> /etc/nginx/ssl/sxzz.moe/sxzz.moe.pem;</span><br><span class="line">    <span class="attribute">ssl_certificate_key</span> /etc/nginx/ssl/sxzz.moe/sxzz.moe.key;</span><br><span class="line"></span><br><span class="line">    <span class="attribute">ssl_ciphers</span> EECDH+CHACHA20:EECDH+AES128:RSA+AES128:EECDH+AES256:RSA+AES256:EECDH+3DES:RSA+3DES:!MD5;</span><br><span class="line">    <span class="attribute">ssl_protocols</span> TLSv1 TLSv1.<span class="number">1</span> TLSv1.<span class="number">2</span>;</span><br><span class="line"></span><br><span class="line">    <span class="attribute">ssl_session_cache</span> shared:SSL:<span class="number">10m</span>;</span><br><span class="line">    <span class="attribute">ssl_session_timeout</span> <span class="number">10m</span>;</span><br><span class="line"></span><br><span class="line">    <span class="attribute">ssl_stapling</span> <span class="literal">on</span>;</span><br><span class="line">    <span class="attribute">ssl_stapling_verify</span> <span class="literal">on</span>;</span><br><span class="line"></span><br><span class="line">    <span class="attribute">add_header</span> Strict-Transport-Security max-age=<span class="number">15552000</span>;</span><br><span class="line"></span><br><span class="line">    <span class="attribute">root</span> /var/www/sxzz.moe;</span><br><span class="line">    <span class="attribute">index</span> index.html index.htm;</span><br><span class="line">    <span class="attribute">server_name</span> sxzz.moe;</span><br><span class="line">    <span class="attribute">location</span> / &#123;</span><br><span class="line">        <span class="comment"># First attempt to serve request as file, then</span></span><br><span class="line">        <span class="comment"># as directory, then fall back to displaying a 404.</span></span><br><span class="line">        <span class="attribute">try_files</span> <span class="variable">$uri</span> <span class="variable">$uri</span>/ =<span class="number">404</span>;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>重启一下 Nginx，大功告成！</p><h2 id="自动更新-acme-sh（引用自官方-Wiki）"><a href="#自动更新-acme-sh（引用自官方-Wiki）" class="headerlink" title="自动更新 acme.sh（引用自官方 Wiki）"></a>自动更新 acme.sh（引用自官方 Wiki）</h2><p>目前由于 acme 协议和 Let’s Encrypt CA 都在频繁的更新, 因此 acme.sh 也经常更新以保持同步。</p><p>升级 acme.sh 到最新版：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">acme.sh --upgrade</span><br></pre></td></tr></table></figure><p>如果你不想手动升级，可以开启自动升级：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">acme.sh --upgrade --auto-upgrade</span><br></pre></td></tr></table></figure><p>之后，acme.sh 就会自动保持更新了。</p><p>你也可以随时关闭自动更新：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">acme.sh --upgrade --auto-upgrade 0</span><br></pre></td></tr></table></figure><h2 id="参考链接"><a href="#参考链接" class="headerlink" title="参考链接"></a>参考链接</h2><ul><li><a href="https://github.com/Neilpang/acme.sh/wiki/%E8%AF%B4%E6%98%8E">官方中文 Wiki</a></li></ul>]]></content>
    
    
    <summary type="html">&lt;p&gt;服务器环境：Ubuntu 16.04 + Nginx&lt;/p&gt;</summary>
    
    
    
    
    <category term="web" scheme="https://blog.sxzz.moe/tags/web/"/>
    
    <category term="https" scheme="https://blog.sxzz.moe/tags/https/"/>
    
  </entry>
  
</feed>
